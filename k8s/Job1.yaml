apiVersion: batch/v1
kind: Job
metadata:
  name: curl-nginx-job
spec:
  template:
    spec:
      containers:
      - name: curl-nginx
        image: radial/busyboxplus:curl
        command:
        - sh
        - -c
        - |
          curl http://nginx-pod:80
      restartPolicy: Never
  backoffLimit: 4
